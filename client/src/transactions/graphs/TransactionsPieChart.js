import { Typography } from "antd";

const TransactionsPieChart = ({ transactions }) => {
    return (
        <Typography.Title level={4}>Pie chart</Typography.Title>
    );
}

export default TransactionsPieChart;