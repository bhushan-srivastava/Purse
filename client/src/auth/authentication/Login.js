const Login = () => {
    return (
        <div>
            Login
            {/* constantly keep showing login (if not logged in)
                and show "/" if logged in */}
        </div>
    );
}

export default Login;